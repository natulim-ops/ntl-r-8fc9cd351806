# Report influencer Natulim IT

Pagina statica generata da `local/it-ads-nc/build-influencer.py` + `standalone.py`.
URL non indicizzato (robots.txt + meta noindex). Non aggiungere altri file qui:
questo repo serve solo a servire `index.html`.

Per aggiornare: `bash local/it-ads-nc/pubblica.sh`
